{
  "name": "arbitrage-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "npx hardhat run scripts/deploy.js --network base",
    "deploy": "npx hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0"
  }
}
